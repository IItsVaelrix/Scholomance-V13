# Secrets — never commit
.env
.env.local

# Python
.venv/
__pycache__/
*.pyc

# Java / Gradle build artifacts
build/
.gradle/
gradle-8.5/
gradle.zip

# Generated data / models / logs
embeddings/
divtube_memory.db
niche_database.sqlite
*.log

# Aether reserve state (per-machine spend tracking)
.aether_meter.json

# Per-tab /prompt conversation memory (DivTube, Mother, …)
.tui_prompt_history.json

# Backups
*.bak
.blackboxcli/
*.consolidation-backup
*.healer.bak

# Editor swap / temp files — never commit
*.kate-swp
*.swp
*~

# Stray crash / bug-report artifacts (screenshots & pasted blobs).
# `bug` is an unanchored 1920x1080 PNG screenshot, not source.
/bug
bug.png
Screenshot_*.png
right-or-wrong*.png
