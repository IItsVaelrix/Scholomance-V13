# Arena Tiles

Base pixel art lives in `base-pixelart/`.
AI enhanced versions (texture upscaled + shaders) live in `enhanced/`.

See PIPELINE.md for the full method.

Current set:
- rune-tile-base + enhanced
- rune-tile-var-a + enhanced
- central-circle-base + enhanced

Add every distinct rune glyph visible in refs/full-reference.jpg as its own base pixel art.
Then enhance each.

To assemble: use placements/arena-layout-v1.json (expand as you add more unique tiles).