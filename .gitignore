# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc coverage files
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-temporary-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Mac files
.DS_Store

# Vite
dist
dist-ssr
.vite
/test-results/
/playwright-report/

# Local data packs and runtime databases (keep local, do not commit)
dict_data/*
!dict_data/.gitkeep
!dict_data/README.md
*.sqlite
*.sqlite-shm
*.sqlite-wal
scholomance_collab_test_*.sqlite
scholomance_collab_test_*.sqlite-shm
scholomance_collab_test_*.sqlite-wal
abyss.sqlite
abyss.sqlite-shm
abyss.sqlite-wal
tmp-*.png
*.png
!godot_project/assets/blocks/*.png
*.bmp

# SCDL compile dumps — regenerable build artifacts (the .scdl is the source, and
# -frameloop.json manifests are kept). Sibling -png.png is already ignored above.
*-json.json
*-phaser.json
*-svg.svg

# Keys
key1
key1.pub

# Windows odd file occasionally created in this workspace
nul

# Large data files (keep local only, use LFS if needed)
english-wordnet-2025.xml
english-wordnet-2025.xml.gz
*test-output.txt
.blackboxcli/
.claude/worktrees/
.worktrees/

# PB-SANI output artifacts (generated, not committed)
scripts/pb-sani/output/

.aider*
diagnostic_report.json

# forced-alignment pipeline (scripts/align_lyrics.py)
.venv-align/
tmp/

# Rust / build output (regenerable — never commit)
target/
**/target/

# Generated media / build artifacts (regenerable — keep local only)
output/videos/*.mp4
out/*.mp4
*.mp4

# Generated pixelbrain / foundry outputs
output/

# Vite build output
build/

# SteamDeck brain runtime
steamdeck_brain/__pycache__/
*.pyc
# SCDNA compiler runtime artifacts (generated at default paths)
steamdeck_brain/vaelrix_forcefield/scdna/compiler.history.jsonl

# Generated assets
generated-assets/
public/generated-assets/
public/assets/*
!public/assets/channel-zero/
!public/assets/channel-zero/*.svg
*-aseprite.aseprite

# Purged oversized artifacts — never commit DB backups or downloaded media (GitHub 100MB limit)
*.sqlite.bak.*
divtube_downloader/downloads/
divtube_downloader/subtlety-spool/

# Electron/AppImage extract (~531 MB) — keep local only
squashfs-root/

# Scratch/probe output. A production ingest token was committed from here on
# 2026-07-23; nothing under .tmp/ is source. (33 pre-existing .tmp/ files stay
# tracked — .gitignore does not untrack, and removing them is a separate call.)
.tmp/

# Career Graph: pinned sources are fetched locally and never committed.
# (config/career-graph-sources.json and tests/fixtures/career-graph ARE committed.)
data/career-graph/raw/
data/career-graph/normalized/
data/career-graph/shards/
data/career-graph/*.sqlite

# Carrier E2E preview renders — regenerate with:
#   node scripts/blender-carrier-e2e.mjs
output/carrier-e2e/

# Gutenberg corpus cache — 5,318 books / 1.9GB, rebuilt by
# scripts/fetch-gutenberg-corpus.mjs. Derived data, not source.
cache/gutenberg/
cache/pg_catalog.csv

# Universal Dependencies English-EWT — gold treebank, fetched by
# scripts/fetch-ud-treebank.mjs. CC BY-SA 4.0, not vendored.
cache/ud/
